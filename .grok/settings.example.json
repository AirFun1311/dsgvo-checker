{
  "_kommentar": "Beispiel fuer projektspezifische Einstellungen (.grok/settings.json). Eintraege in die echte settings.json uebernehmen, die _kommentar-Felder dabei weglassen. Geheimnisse gehoeren NICHT hierher - per ${ENV_VAR} referenzieren.",

  "mcpServers": [
    {
      "name": "filesystem",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."]
    },
    {
      "name": "github",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  ]
}
